import React from 'react';
import { TitleWrapper, Title } from './titleElements';

const Titlebar = () => {
  return (
    <TitleWrapper to='/'>
      <Title>
        nodetok
      </Title>
    </TitleWrapper>
  )
}

export default Titlebar